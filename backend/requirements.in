# GPU VM 설치: bash backend/scripts/install_requirements.sh
# (pip install -r requirements.txt 단독은 gptqmodel 빌드 실패 가능)
fastapi
uvicorn[standard]
python-multipart

# =========================
# Config / Environment
# =========================
python-dotenv
pydantic-settings
PyYAML

# =========================
# Logging
# =========================
loguru
psutil

# =========================
# Image Processing
# =========================
pillow

# 폰트 번들: pip install 후 `python scripts/setup_fonts.py`

# =========================
# Testing
# =========================
pytest
httpx

# =========================
# AI Providers / PyTorch (GPU / CUDA)
# =========================
# torch/torchvision/torchaudio는 install_requirements.sh 1단계(requirements-torch.txt)에서 선설치
# torch 2.8.0 (gptqmodel VLM) + xformers 0.0.32.post2 (torch 2.8.0 공식 wheel)
torch==2.8.0
torchvision==0.23.0
torchaudio==2.8.0
#
# HuggingFace / Diffusers 기반 이미지 생성
# - SDXL Lightning 4-step
openai
diffusers
transformers
accelerate
safetensors
huggingface-hub
sentencepiece
protobuf
xformers==0.0.32.post2

# Poster design VLM (Qwen2-VL GPTQ) — torch>=2.8 필요
qwen-vl-utils
optimum
gptqmodel==5.8.0
torchao==0.16.0

# pytest traceback / terminal output formatting
Pygments

# Image background removal (poster layout analysis)
# 2.0.76 은 numpy>=2.3 요구 → gptqmodel 5.8(numpy==2.2.6)과 충돌
rembg[gpu]==2.0.67
# 1.27+ 는 CUDA 13 빌드 → torch 2.8.0+cu128 환경에서 libcudart.so.13 오류
onnxruntime-gpu==1.26.0

# numba/pymatting compatibility for rembg; gptqmodel 5.8 pins numpy==2.2.6
numpy==2.2.6

# gptqmodel→datasets 는 fsspec<=2026.4.0 요구 (torch compile 시 2026.6.0 과 충돌)
fsspec==2026.4.0

# TestClient dependency for Starlette/FastAPI tests
httpx2

# =========================
# Auth / Social Login
# =========================
PyJWT
